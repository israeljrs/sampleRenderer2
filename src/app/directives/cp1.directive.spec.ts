import { Cp1Directive } from './cp1.directive';

describe('Cp1Directive', () => {
  it('should create an instance', () => {
    const directive = new Cp1Directive();
    expect(directive).toBeTruthy();
  });
});
